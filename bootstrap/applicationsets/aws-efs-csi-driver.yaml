apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: aws-efs-csi-driver
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - clusters:
        selector:
          matchLabels:
            argocd.argoproj.io/secret-type: cluster
  template:
    metadata:
      name: aws-efs-csi-driver-{{name}}
      annotations:
        argocd.argoproj.io/sync-wave: "0"
    spec:
      project: default
      sources:
        - repoURL: https://github.com/slikk66/eks-argo-bootstrap
          targetRevision: HEAD
          ref: values
        - chart: aws-efs-csi-driver
          repoURL: https://kubernetes-sigs.github.io/aws-efs-csi-driver
          targetRevision: 4.1.0
          helm:
            releaseName: aws-efs-csi-driver
            ignoreMissingValueFiles: true
            valueFiles:
              - $values/charts/aws-efs-csi-driver/values.yaml
            valuesObject:
              controller:
                serviceAccount:
                  annotations:
                    eks.amazonaws.com/role-arn: '{{metadata.annotations.efs_csi_role_arn}}'
                tags:
                  "efs.csi.aws.com/cluster": '{{metadata.annotations.cluster_name}}'
      destination:
        name: '{{name}}'
        namespace: kube-system
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
